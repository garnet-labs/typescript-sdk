/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f2806bd9c69c
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Image generation call is generating
 */
export type ImageGenCallGeneratingEvent = {
  itemId: string;
  outputIndex: number;
  sequenceNumber: number;
  type: "response.image_generation_call.generating";
};

/** @internal */
export const ImageGenCallGeneratingEvent$inboundSchema: z.ZodType<
  ImageGenCallGeneratingEvent,
  unknown
> = z.object({
  item_id: z.string(),
  output_index: z.int(),
  sequence_number: z.int(),
  type: z.literal("response.image_generation_call.generating"),
}).transform((v) => {
  return remap$(v, {
    "item_id": "itemId",
    "output_index": "outputIndex",
    "sequence_number": "sequenceNumber",
  });
});

export function imageGenCallGeneratingEventFromJSON(
  jsonString: string,
): SafeParseResult<ImageGenCallGeneratingEvent, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ImageGenCallGeneratingEvent$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ImageGenCallGeneratingEvent' from JSON`,
  );
}
